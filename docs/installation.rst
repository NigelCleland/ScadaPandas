============
Installation
============

At the command line::

    $ easy_install ScadaPandas

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ScadaPandas
    $ pip install ScadaPandas